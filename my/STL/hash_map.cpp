#include <bits/stdc++.h>

template<typename KEY, typename VALUE>
class hash_map {
    using Point = std::map<KEY, VALUE>*;
private:
    Point *heap;
    size_t m_size;
    size_t m_capacity;
public:

};

int main() {
    std::unordered_map<int, int> mp;
    mp.
}