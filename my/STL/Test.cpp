#include <bits/stdc++.h>
#include "Array.cpp"

int main() {
    std::array<int, 3> a;
    Array<int, 3> b;
    a.fill(10);
    b.fill(10);
    const auto it = a.crbegin();
    const auto it1 = b.crbegin();
}