#include <bits/stdc++.h>

void solve() {
    int n;
    std::cin >> n;
    std::cout << "+ " << n - 1 << std::endl;
    std::cin >> n;
    while(1) {
        int k = 0;
        char op = '+';
        std::cout << op << ' ' << (1LL << k++) << std::endl;
        op = 
    }


}

int main() {
    std::ios::sync_with_stdio(0);
    std::cin.tie(0);
    int T = 1;
    //std::cin >> T;
    while(T--) solve();
    return 0;
}
