#include<bits/stdc++.h>

signed main(){
    std::ios::sync_with_stdio(false),std::cin.tie(nullptr);
    int t;std::cin>>t;
    while(t--){
        int a,b;
        std::cin>>a>>b;
        std::cout<<2<<'\n'<<a-1<<' '<<1<<'\n'<<a<<' '<<b<<'\n';
    }
    return 0;
}