#include <bits/stdc++.h>

constexpr int N = 1005;
constexpr int mod = 10301;

int dp[N][N];

signed main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    
    int n, a, b;
    std::cin >> n >> a >> b;
    for(int i = 1; i <= n; ++i) {

    }

    return 0;
}