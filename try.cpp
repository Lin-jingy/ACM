#include <bits/stdc++.h>

double PI = acos(-1);

void solve() {
    std::cout << sin(0.25 * PI) / 2;
}

signed main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    int T = 1;
    // std::cin >> T;
    while(T--) solve();
    return 0;
}